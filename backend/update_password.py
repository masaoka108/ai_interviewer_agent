from app.db.session import SessionLocal; from app.models.user import User; from app.core.security import get_password_hash; def update_password(): db = SessionLocal(); user = db.query(User).filter(User.email == "tech@example.com").first(); user.hashed_password = get_password_hash("company123") if user else None; db.commit() if user else None; print("Password updated successfully") if user else print("User not found"); update_password()
